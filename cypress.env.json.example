{
  "_comment": "Copy this file to cypress.env.json and fill in real values. cypress.env.json is gitignored. In CI these come from CYPRESS_USER_EMAIL / CYPRESS_USER_PASSWORD secrets instead.",
  "userEmail": "your.account@example.com",
  "userPassword": "your-password"
}
